{% extends "base.html.twig" %}
{% block style %}<link href="css/style-signin.css" rel="stylesheet">

{% endblock %}
{% block title %} {{ parent() }} catalogue {% endblock %}

{% block contenu %}

      <style >


h1 {
  font-size: 1.5em;
  font-weight: normal;
  color: #333;
  margin:-15px 0 0 0;
  clear:both;
}
.viini-kortti {
  width: 190px;
  height: 190px;
  overflow: hidden;
  float: left;
  margin: 0 5px 5px 0;
  -webkit-box-shadow: inset 0px 0px 20px 0px rgba(0, 0, 0, 0.3);
  box-shadow: inset 0px 0px 20px 0px rgba(0, 0, 0, 0.3);
  -webkit-transition:all 0.3s ease-in;
   -moz-transition:all 0.3s ease-in;
   -o-transition:all 0.3s ease-in;
   transition:all 0.3s ease-in;
}
.viini-kortti h2 {
     color: #fff;
  font-size: 0.8em;
  font-weight:normal;
  font-size: 0.9em;
  padding: 10px 20px 10px 20px;
  margin: 150px 0 0 0;
  background: rgba(0,0,0,0.3);
  -webkit-transition:margin-top 0.3s ease-in;
  -moz-transition:margin-top 0.3s ease-in;
  -o-transition:margin-top 0.3s ease-in;
  transition:margin-top 0.3s ease-in;
}
.viini-kortti ul {
     color: #fff;
  font-size: 0.8em;
  list-style: none;
  margin: 0;
  padding: 0;
  background: rgba(0,0,0,0.3);
  margin: 0;
  padding-left: 20px;
  padding-bottom: 50px
}
.viini-kortti:hover h2 {
  margin-top: 40px;
}

/*colors*/
.p-livre {

  background-color: #cccccc;

 background-position: center;
 background-repeat: no-repeat;
 background-size:contain;
 position: relative;
}



  </style>


    <div class="container" style="margin-top:2em;margin-bottom:2em;margin-left:1em;">
  <h1>Punaiset</h1>
<!-- punkut-->
 {% for c in liste %}
<div class="viini-kortti p-livre">
  <h2>{{l.titre}}</h2>
  <ul>
    <li>{{a.nom}},{{a.prenom}}</li>
    <li>{{d.libelle}}</li>
    <li>{{l.prix}}</li>
  </ul>
</div>
  {% endfor %}



</div><!-- container-->

{% endblock %}
